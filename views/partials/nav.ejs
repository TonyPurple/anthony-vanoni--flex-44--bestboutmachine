</head>

<body>
    <section class="section is-family-monospace">
        <div class="container has-text-justified">
            <nav class="navbar is-transparent" role="navigation" aria-label="main navigation">
                <div class="navbar-brand">
                    <a class="navbar-item button" href="/">Best Bout Machine</a>
                    <a role="button" class="navbar-burger" id="burger" aria-label="menu" aria-expanded="false">
                        <span></span>
                        <span></span>
                        <span></span>
                    </a>
                </div>
                <div class="navbar-menu" id="nav-links">
                    <div class="navbar-start">
                        <a class="navbar-item" href="/matches">Recent Nominations</a>
                        <a class="navbar-item" href="/matches/new">Nominate a Match</a>
                        <a class="navbar-item" href="/promotions/index">View Promotions</a>
                        <a class="navbar-item" href="/posts">Promo Board</a>
                    </div>
                    <div class="navbar-item has-dropdown is-hoverable" id="dropdown">
                        <div class="dropdown-trigger">
                            <button class="navbar-item button" aria-haspopup="true" aria-controls="dropdown-menu" id="dropdownTrigger"> 
                                <span>Sign In/Out</span>
                                </button>
                        </div>
                        <% if (user) { %>
                            <div class="navbar-dropdown">
                                <div class="dropdown-content">
                                    <a class="navbar-item" href="/profiles/<%= user.profile._id %>">View Profile</a>
                                    <a class="navbar-item" href="/profiles">View Users</a>
                                    <a class="navbar-item" href="/auth/logout">Logout</a>
                                </div>
                            </div>
                            <% } else { %>
                                <div class="navbar-dropdown">
                                    <div class="dropdown-content">
                                        <a class="navbar-item" href="/auth/google">Sign In w/ Google</a>
                                    </div>
                                </div>
                    </div>
                    <% } %>
            </nav>
            <br>
            <div id="greeting">
                <% if (user?.profile.avatar) { %>
                    <img src="<%= user.profile.avatar %>" alt="<%= user.profile.name %> avatar" height="28" width="28">
                    <% } else { %>
                        <img src="https://i.imgur.com/tNbIrh8s.jpg" height="28" width="28">
                        <% } %>
                            <strong>hello,
                        <%= user ? user.profile.name : "mark" %>
                    </strong>
            </div>
            <section class="hero is-small is-warning">
                <div class="hero-body">
                    <h1 class="title is-pulled-right">
                        <%= title %>
                    </h1>
                </div>
            </section>
            <br>