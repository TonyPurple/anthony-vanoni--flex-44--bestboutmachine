</head>

<body>
    <section class="section is-family-monospace">
        <div class="container has-text-justified">
            <nav class="navbar" role="navigation" aria-label="main navigation">
                <div class="navbar-brand">
                    <div class="navbar-start">
                        <a class="navbar-item button is-dark is-outlined is-rounded" href="/">Best Bout Machine</a>
                        <a class="navbar-item button is-light is-inverted is-rounded" href="/matches">Recent Nominations</a>
                        <a class="navbar-item button is-light is-inverted is-rounded" href="/matches/new">Nominate a Match</a>
                        <a class="navbar-item button is-light is-inverted is-rounded" href="/promotions/new">ADD Promotion</a>
                    </div>
                    <div class="navbar-end">
                        <div class="navbar-item">
                            <form action="/matches" method="GET">
                                <input class="search" name="search" type="search" placeholder="Search Matches">
                            </form>
                            <strong>hello,
                            <%= user ? user.profile.name : "mark" %></strong>
                            <% if (user?.profile.avatar) { %>
                                <div class="navbar-item">
                                    <img src="<%= user.profile.avatar %>" alt="<%= user.profile.name %> avatar">
                                </div>
                                <% } %>
                                    <% if (user) { %>
                                        <a class="navbar-item button is-light is-inverted is-rounded" href="/profiles/<%= user.profile._id %>">View Profile</a>
                                        <a class="navbar-item button is-light is-inverted is-rounded" href="/profiles">View Users</a>
                                        <a class="navbar-item button is-light is-inverted is-rounded" href="/auth/logout">Logout</a>
                                        <% } else { %>
                                            <div class="navbar-item">
                                                <img src="https://i.imgur.com/tNbIrh8s.jpg" height="28" width="28">
                                            </div>
                                            <a class="navbar-item button is-dark is-outlined is-rounded" href="/auth/google">Sign In w/ Google</a>
                                            <% } %>
                        </div>
                    </div>
                </div>
            </nav>
            <br>
            <section class="hero is-small is-warning">
                <div class="hero-body">
                    <h1 class="title is-pulled-right">
                        <%= title %>
                    </h1>
                </div>
            </section>
            <br>